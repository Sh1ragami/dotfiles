source = ~/.config/hypr/monitors.conf
# monitor=,preferred,auto,auto

$terminal = kitty
$fileManager = /home/sh1ragami/.config/hypr/scripts/toggle_yazi.sh
$fileManagerSecond = thunar
$menu = wofi
$logout = wlogout
$mainBrowser = firefox
$chat = class:^teams-for-linux$
$keyboardSounds = /home/sh1ragami/.config/waybar/scripts/vibes-menu.sh
$networkSettings = /home/sh1ragami/.config/hypr/scripts/toggle_wifi.sh

exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = awww-daemon && sleep 2 && awww img /home/sh1ragami/Pictures/wallpaper3.jpg --outputs "eDP-1,HDMI-A-1"
exec-once = waybar > /dev/null 2>&1
exec-once = conky --config ~/.config/conky/conky.conf
exec-once = nm-applet --indicator
exec-once = fcitx5 -d -r
exec-once = batsignal -w 15 -c 5 -d 2
exec-once = hypridle
exec-once = swaync
exec-once = /home/sh1ragami/.config/hypr/scripts/close_on_move.sh
exec-once = /home/sh1ragami/.config/hypr/scripts/wallpaper_refresh.sh
#exec-once = ~/.config/waybar/scripts/wayvibes_toggle.sh


env = HYPRCURSOR_THEME,catppuccin-mocha-dark-cursors
env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME,catppuccin-mocha-dark-cursors
env = XCURSOR_SIZE,24
#env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = QT_QPA_PLATFORM,wayland;xcb
env = GDK_BACKEND,wayland,x11
env = SDL_VIDEODRIVER,wayland
env = CLUTTER_BACKEND,wayland
env = XMODIFIERS, @im=fcitx
env = QT_IM_MODULE, fcitx
# env = GTK_IM_MODULE, fcitx
env = QT_QPA_PLATFORMTHEME,qt6ct
env = MOZ_ENABLE_WAYLAND, 1
env = SDL_IM_MODULE, fcitx

general {
    gaps_in = 0
    gaps_out = 0
    border_size = 1 

    col.active_border = rgba(4c86fbee)
    col.inactive_border = rgba(3d484daa)   

    layout = dwindle

    # 境界線のドラッグによるリサイズを有効化
    resize_on_border = true

    # 境界線のつかみ判定を広げる（ピクセル単位）
    # デフォルトのままだと細すぎてつかみにくい場合があるので設定推奨
    extend_border_grab_area = 15
}    # 境界線のドラッグによるリサイズを有効化
    resize_on_border = true

    # 境界線のつかみ判定を広げる（ピクセル単位）
    # デフォルトのままだと細すぎてつかみにくい場合があるので設定推奨
    extend_border_grab_area = 25

decoration {
    rounding = 5
    #inactive_opacity = 0.8

    shadow {
        enabled = true
        range = 20
        render_power = 4
        color = rgba(00000066)
    }

    blur {
        enabled = true
        size = 1
        passes = 3
        new_optimizations = true
        ignore_opacity = true
        xray = true
    }
}

animations {
    enabled = true
    bezier = fluid, 0.15, 0.85, 0.25, 1
    bezier = snappy, 0.3, 1, 0.4, 1
    animation = windows, 1, 3, fluid, popin 5%
    animation = windowsOut, 1, 2.5, snappy
    animation = fade, 1, 4, snappy
    animation = workspaces, 1, 1.7, snappy, slide
    animation = specialWorkspace, 1, 4, fluid, slidefadevert -35%
    animation = layers, 1, 2, snappy, popin 70%
}

dwindle {
    # pseudotile = true
    preserve_split = true
}

misc {
    force_default_wallpaper = 0
    disable_hyprland_logo = true
}

input {
    kb_layout = us
    kb_options = ctrl:nocaps,altwin:swap_ralt_rwin
    follow_mouse = 1
    sensitivity = 0
    touchpad {
        natural_scroll = true
        scroll_factor = 0.3
    }
}

$mainMod = SUPER

bind = $mainMod, T, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exec, pkill $logout || $logout
bind = $mainMod, O, exec, obsidian
bind = $mainMod, G, exec, google-chrome-stable --app=https://gemini.google.com/

bind = $mainMod, E, exec, $fileManager
bind = $mainMod ctrl, E, exec, pkill $fileManagerSecond || $fileManagerSecond

# bind = $mainMod, A, exec, anki

bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, pkill $menu || $menu --show drun
bind = $mainMod, P, pseudo,
bind = $mainMod, J, layoutmsg, togglesplit,
bind = $mainMod, F, fullscreen
bind = $mainMod, B, exec, $mainBrowser
bind = $mainMod, W, exec, pkill -USR1 waybar
bind = , Print, exec, sh -c 'pkill slurp || { mkdir -p ~/Pictures/Screenshots && AREA=$(slurp) && [ -n "$AREA" ] && sleep 0.15 && grim -g "$AREA" - | tee ~/Pictures/Screenshots/screenshot_$(date +%Y%m%d_%H%M%S).png | wl-copy; }'


bind = $mainMod, Z, exec, pkill $keyboardSounds | $keyboardSounds
bind = $mainMod, N, exec, pkill $networkSettings | $networkSettings


# Super + 右クリックドラッグでウィンドウをリサイズ
bindm = $mainMod, mouse:273, resizewindow
# Super + 左クリックドラッグでウィンドウを移動
bindm = $mainMod, mouse:272, movewindow

bind = $mainMod, X, exec, hyprctl clients | grep -q "chrome-pjbejonmbmbimhjphopcciclekgnpnpj-Default" && hyprctl dispatch focuswindow "class:^chrome-pjbejonmbmbimhjphopcciclekgnpnpj-Default$" || google-chrome-stable --app-id=pjbejonmbmbimhjphopcciclekgnpnpj
bind = $mainMod, C, exec, hyprctl clients | grep -q $chat && hyprctl dispatch focuswindow $chat
bind = $mainMod, C, exec, hyprctl clients | grep -q "teams-for-linux" && hyprctl dispatch focuswindow $chat || teams-for-linux

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = $mainMod ctrl, left, movewindow, l
bind = $mainMod ctrl, right, movewindow, r
bind = $mainMod ctrl, up, movewindow, u
bind = $mainMod ctrl, down, movewindow, d
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d
bind = $mainMod ctrl, h, movewindow, l
bind = $mainMod ctrl, l, movewindow, r
bind = $mainMod ctrl, k, movewindow, u
bind = $mainMod ctrl, j, movewindow, d

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

bind = $mainMod ctrl, 1, movetoworkspace, 1
bind = $mainMod ctrl, 2, movetoworkspace, 2
bind = $mainMod ctrl, 3, movetoworkspace, 3
bind = $mainMod ctrl, 4, movetoworkspace, 4
bind = $mainMod ctrl, 5, movetoworkspace, 5
bind = $mainMod ctrl, 6, movetoworkspace, 6
bind = $mainMod ctrl, 7, movetoworkspace, 7
bind = $mainMod ctrl, 8, movetoworkspace, 8
bind = $mainMod ctrl, 9, movetoworkspace, 9
bind = $mainMod ctrl, 0, movetoworkspace, 10

bind = $mainMod ctrl, left,  swapwindow, l
bind = $mainMod ctrl, right, swapwindow, r
bind = $mainMod ctrl, up,    swapwindow, u
bind = $mainMod ctrl, down,  swapwindow, d
bind = $mainMod ctrl, H, swapwindow, l
bind = $mainMod ctrl, L, swapwindow, r
bind = $mainMod ctrl, K, swapwindow, u
bind = $mainMod ctrl, J, swapwindow, d


bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle

bindel = , XF86MonBrightnessUp, exec, brightnessctl set 5%+
bindel = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

bindel = , XF86KbdBrightnessUp, exec, brightnessctl -d *kbd* s 10%+
bindel = , XF86KbdBrightnessDown, exec, brightnessctl -d *kbd* s 10%-

xwayland {
    force_zero_scaling = true
}

# WiFi設定(nmtui)をフローティングにする
windowrule = float on, match:class nmtui-float
windowrule = size 900 600, match:class nmtui-float
windowrule = center on, match:class nmtui-float


# Yazi (Kitty) 用のフローティング設定
windowrule = float on, match:class yazi-float
windowrule = size 900 600, match:class yazi-float
windowrule = center on, match:class yazi-float

windowrule = float on, match:class ^(chrome-pjbejonmbmbimhjphopcciclekgnpnpj-Default)$
windowrule = size 900 600, match:class ^(chrome-pjbejonmbmbimhjphopcciclekgnpnpj-Default)$
windowrule = center on, match:class ^(chrome-pjbejonmbmbimhjphopcciclekgnpnpj-Default)$

# Thunar 用のフローティング設定
windowrule = float on, match:class thunar
windowrule = size 1000 700, match:class thunar
windowrule = center on, match:class thunar

# SwayNC のブラー設定 (最新の match:namespace 形式)
layerrule = blur on, match:namespace swaync-control-center
layerrule = ignore_alpha 0.5, match:namespace swaync-control-center
layerrule = blur on, match:namespace swaync-notification-window
layerrule = ignore_alpha 0.5, match:namespace swaync-notification-window

device {
    name = elan-trackpoint
    sensitivity = 0.4
    accel_profile = flat

    scroll_factor = 0.7
}

device {
    name = logitech-g300s-optical-gaming-mouse
    sensitivity = -0.5
    # accel_profile = flat
    # scroll_factor = 1
}

